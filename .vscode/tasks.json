{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"label": "Test",
			"command": "/usr/bin/gcc",
			"args": [
				"-fdiagnostics-color=always",
				"-g",
				"-pthread",
				"${file}",
				"-o",
				"${workspaceFolder}/a.out"
			],
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": "build",
			"detail": "compiler: /usr/bin/gcc"
		}
	]
}